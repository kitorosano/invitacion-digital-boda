---
interface Props {
  message: string;
  colorsToAvoid: string[];
}
const { message, colorsToAvoid } = Astro.props;
const img = "/images/dress_code.svg";
---

<div class="dresscode-container">
  <h2>Vestimenta formal</h2>
  <p>{message}</p>

  <div class="dresscode-restrictions">
    <section class="restrictions-men">
      <ul>
        <h3>Hombre</h3>
        <li>Traje clásico o camisa lisa</li>
        <li>Corbata o moño</li>
        <li>Zapatos</li>
      </ul>
    </section>

    <picture>
      <img src={img} alt="Dress Code" />
    </picture>

    <section class="restrictions-women">
      <ul>
        <h3>Mujer</h3>
        <li>Vestido largo o midi, enterizo, traje</li>
        <li>Zapatos o sandalias</li>
      </ul>
    </section>
  </div>

  <p>
    Te pedimos amablemente que <span>no utilices</span> estos colores en tu vestimenta.
  </p>

  <ul>
    <li style={`background:${colorsToAvoid[0]}`}></li>
    <li style={`background:${colorsToAvoid[1]}`}></li>
    <li style={`background:${colorsToAvoid[2]}`}></li>
  </ul>
</div>

<style>
  .dresscode-container {
    margin: 3rem auto 0 auto;
    padding: 0.5rem 0;
    width: 100%;
    text-align: center;
  }

  .dresscode-container > h2 {
    margin: 0 auto;

    font-size: var(--text-size-lg);
    font-weight: 600;
    color: var(--color-dark);
    text-align: center;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .dresscode-container p {
    margin: 1rem auto 0 auto;

    font-size: var(--text-size-md);
    font-weight: 400;
    color: var(--color-dark);

    max-width: 80%;
    text-wrap: pretty;
    line-height: 1.5;
    letter-spacing: 0.02em;
  }

  .dresscode-container p span {
    font-weight: 600;
  }

  .dresscode-restrictions {
    width: 100%;
    height: 100%;

    min-height: 270px; // same as picture height

    margin: 1rem auto 0 auto;
    padding: 0 auto;

    display: flex;
    position: relative;
  }

  .restrictions-men,
  .restrictions-women {
    width: 100%;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
  }

  .restrictions-men > ul,
  .restrictions-women > ul {
    width: 50%;
    padding: 0;

    list-style: none;
    text-align: left;
  }
  .restrictions-men > ul {
    margin: 0rem auto 0 1rem;
  }

  .restrictions-women > ul {
    margin: 0rem 1rem 0 auto;
  }

  .restrictions-men > ul h3,
  .restrictions-women > ul h3 {
    margin: 1rem auto;

    font-family: var(--font-secondary);
    font-size: var(--text-size-lg);
    font-weight: 500;
    font-style: italic;
    color: var(--color-dark);
    text-align: center;
    letter-spacing: 0.05rem;

    max-width: 100%;
    text-wrap: balance;
  }

  .restrictions-men > ul li,
  .restrictions-women > ul li {
    margin: 1rem 0;

    font-size: var(--text-size-sm);
    font-weight: 500;
    color: var(--color-dark);

    text-wrap: balance;
    letter-spacing: 0.02rem;
  }

  .restrictions-men > ul li {
    text-align: center;
  }
  .restrictions-women > ul li {
    text-align: center;
  }

  .dresscode-restrictions > picture {
    margin: 0 auto;

    width: 100%;
    height: 270px;
    display: block;
    overflow: hidden;
    position: absolute;
    z-index: -1;
  }

  .dresscode-restrictions > picture img {
    position: absolute;
    top: 0;
    left: 0%;

    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    pointer-events: none;

    filter: brightness(0.9);
  }

  .dresscode-container > ul {
    margin: 1.5rem auto 0 auto;
    padding: 0;

    display: inline-block;
    position: relative;

    width: 1px;
    height: 3rem;
    list-style: none;
  }

  .dresscode-container > ul li {
    width: 4rem;
    height: 4rem;
    border-radius: 50%;
    z-index: 1;

    position: absolute;
  }

  .dresscode-container > ul li:nth-child(1) {
    left: -5rem;
    z-index: 1;
  }
  .dresscode-container > ul li:nth-child(2) {
    left: -2rem;
    z-index: 2;
  }
  .dresscode-container > ul li:nth-child(3) {
    left: 1rem;
    z-index: 3;
  }

  @media (min-width: 768px) {
  }

  @media (min-width: 1024px) {
    .dresscode-container > h2 {
      font-size: var(--text-size-xl);
    }

    .dresscode-restrictions {
      min-height: 350px; // same as picture height
    }

    .restrictions-men,
  .restrictions-women {
    margin-top: 4rem;
  }

    .restrictions-men > ul,
    .restrictions-women > ul {
      width: 100%;
    }
      .restrictions-men > ul {
    margin: 0rem auto 0 1rem;
  }

  .restrictions-women > ul {
    margin: 0rem 1rem 0 auto;
  }

    .dresscode-restrictions > picture {
      height: 350px;
    }


  }
</style>
