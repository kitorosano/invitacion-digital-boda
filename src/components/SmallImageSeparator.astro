---
interface Props {
  src: string;
}

const { src } = Astro.props;
---

<div class="small-image-separator-container">
  <picture>
    <img src={src} alt="Separador decorativo" />
  </picture>
</div>

<style>
  .small-image-separator-container {
    width: 100%;
    position: relative;
    margin: 2rem 0 0 0;
  }

  .small-image-separator-container > picture {
    width: 100%;
    height: 200px;
    display: block;
    overflow: hidden;
    position: relative;
  }

  .small-image-separator-container > picture > img {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);

    width: 110%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 50%;
    pointer-events: none;

    filter: brightness(0.9);
  }

  .small-image-separator-container::before {
    content: "";
    position: absolute;
    top: -1px;
    left: 0;
    width: 100%;
    z-index: 1;

    height: 20px;
    background: linear-gradient(to top, transparent, var(--bg-color));
  }
  .small-image-separator-container::after {
    content: "";
    position: absolute;
    bottom: -1px;
    left: 0;
    width: 100%;
    z-index: 1;

    height: 50px;
    background: linear-gradient(to top, var(--bg-color), transparent);
  }

  @media (min-width: 576px) {
    .small-image-separator-container picture {
      height: 300px;
    }

    .small-image-separator-container picture > img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: 50% 40%;
    }
  }

  @media (min-width: 1024px) {
    .small-image-separator-container {
      margin: 4rem 0 0 0;
    }

    .small-image-separator-container picture {
      height: 450px;
    }

    .small-image-separator-container picture > img {
      object-position: 50% 35%;
    }
  }
</style>
